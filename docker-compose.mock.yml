services:
  trading-api:
    build:
      context: ./app
      args:
        REQUIREMENTS: requirements-mock.txt
    container_name: trading-api-mock
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
